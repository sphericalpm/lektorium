lektorium-deploy:
  variables:
    LC_ALL: C.UTF-8
    LANG: C.UTF-8
  script:
    - apk add --update python3 python3-dev libffi-dev openssl-dev build-base
    - pip3 install -e .[dev,inv]
    - cp ${LEKTORIUM_DEPLOY_CONFIG} invoke.yaml
    - inv deploy
  only:
    - master
