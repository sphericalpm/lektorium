lektorium-deploy:
  variables:
    LC_ALL: C.UTF-8
    LANG: C.UTF-8
    GIT_STRATEGY: clone
    GIT_DEPTH: 0
  script:
    - cat /etc/alpine-release
    - apk add --update python3 git docker-cli bash
    - apk add --update py3-pip || true
    - python3 -m pip install -U pip
    - git tag && git status && git describe
    - pip install 'spherical-dev[dev]>=0.2.2,<0.3.0' wheel
    - cp ${LEKTORIUM_DEPLOY_CONFIG} invoke.yaml
    - inv deploy
    - docker ps -a
  only:
    - master

stages:
  - sync

manual_sync:
  stage: sync
  image: alpine:latest
  script:
    - apk add --no-cache git rsync openssh-client

    - git remote set-url origin https://oauth2:${CI_ACCESS_TOKEN}@code.iarc.fr/research-project-sites/lektorium.git

    - git config --global user.name "GitLab CI"
    - git config --global user.email "ci@iarc.who.int"

    - git clone https://github.com/sphericalpm/lektorium.git /tmp/lektorium

    - rsync -av --delete --exclude='.git' /tmp/lektorium/ ./

    - git diff

    - git commit -am 'Sync with github'
    - git push origin HEAD:${CI_COMMIT_REF_NAME}

  only:
    - web
